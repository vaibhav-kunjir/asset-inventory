# Asset Inventory Scanner - Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# PostgreSQL Database Connection
# ============================================
# Your existing asset inventory database

# For Docker deployment (connecting to host PostgreSQL)
DB_HOST=host.docker.internal
DB_PORT=4432

# For local development
# DB_HOST=localhost
# DB_PORT=5432

DB_NAME=novaapi
DB_USER=postgres
DB_PASSWORD=postgres

# ============================================
# Vulnerability Database Configuration
# ============================================

# For Docker deployment (bind mounted to current directory)
VULN_DB_PATH=/data/vuln.db

# For local development
# VULN_DB_PATH=./vuln.db

# ============================================
# NIST NVD API Configuration
# ============================================
# Optional: Get free API key to increase rate limit from 5 to 50 requests/30s
# Get your key at: https://nvd.nist.gov/developers/request-an-api-key
# NVD_API_KEY=your-api-key-here

# SSL Verification (set to false if you have certificate issues)
VERIFY_SSL=true

# ============================================
# Flask Application Configuration
# ============================================
# Flask will run on port 5000 by default
# FLASK_ENV=production
# FLASK_DEBUG=false

# ============================================
# Notes
# ============================================
# 1. For Docker: Use host.docker.internal (Mac/Windows) or 172.17.0.1 (Linux)
# 2. For local: Use localhost
# 3. Ensure PostgreSQL is accessible from the container/host
# 4. The VULN_DB_PATH must point to where vuln.db is located
# 5. Never commit .env files with real credentials to version control

